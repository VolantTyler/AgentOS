# Suite: full

## Purpose

Run the broadest saved regression coverage the repository currently supports.

## Selection rules

Include:

- all active manifests under `docs/testing/features/`, except `TEMPLATE.md`, and  
- any additional suite-specific manifests added later.

## Current members

- `feature-quality-system`
- `lead-tracker`
- `job-fit-tracker`
- `requirement-map`
- `tech-stack-updates`
- `ai-news`
- `email-ingest`

## Expected use

Use:

- before merge when the blast radius is broad,  
- after infrastructure or workflow changes that may affect several features, or  
- when Tyler asks for the fullest available saved feature-test run.
